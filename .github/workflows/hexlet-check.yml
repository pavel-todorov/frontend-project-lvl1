name: frontend-project-lvl1-never-been

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:        
      - uses: actions/checkout@v2
          
      - name: Install
        run: |
          make install      
          
      - name: Run linter
        run: |
          make lint
        
      - name: Hexlet project check
        uses: hexlet/project-action@release
        
